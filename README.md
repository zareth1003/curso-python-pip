# GAME PROJECT

para correl el juego debes seguir las instrucciones en la terminal:

```sh
cd game
python main.py
```

# APP project

```
sh
git clone
cd app
env\Scripts\activate
pip install -r requirements.txt
python main.py
```
